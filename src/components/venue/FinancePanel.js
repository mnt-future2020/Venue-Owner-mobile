import { View } from "react-native";

// Stub — venue mobile already has a dedicated Finance tab
// at (tabs)/finance. ProfileScreenContent renders <FinancePanel /> in
// the More menu for player view — venue owner finance lives in its tab,
// so render nothing here.
export default function FinancePanel() {
  return <View />;
}
