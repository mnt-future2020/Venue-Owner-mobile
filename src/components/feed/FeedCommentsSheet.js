// Stub — venue app has no feed/comments. PostsGrid renders this when a
// post is opened; in the venue app the user can't have posts, so this is
// never mounted in practice.
export default function FeedCommentsSheet() {
  return null;
}
